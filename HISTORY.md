# HISTORY.md -- narrative archive

> Consulted only when a session needs to know why a decision was made, not on
> every open. For what's current, read `STATE.md` instead.

## 2026-08-30 -- repo created (UBI-222)

DigitalOcean's own SDK bindings, generated from the real, pinned
`ubx-schema-digitalocean` v1.0.0 release, driven through
`ubx-provider-runbook`'s own `/onboard-provider` command -- the first
real end-to-end run of that runbook (UBI-222), after DigitalOcean's own
schema and pin work (UBI-216/UBI-217) had already proven the mechanism
by hand.
