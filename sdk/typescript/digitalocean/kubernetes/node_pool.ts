// Code generated by `ubx sdk gen` from digitalocean_node_pool. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface NodePool_NodePool_Nodes_Status {
  state?: string | Computed<string>;
}

export interface NodePool_NodePool_Nodes {
  createdAt?: string | Computed<string>;
  dropletId?: string | Computed<string>;
  id?: string | Computed<string>;
  name?: string | Computed<string>;
  status?: NodePool_NodePool_Nodes_Status | Computed<NodePool_NodePool_Nodes_Status>;
  updatedAt?: string | Computed<string>;
}

export interface NodePool_NodePool_Taints {
  effect?: string | Computed<string>;
  key?: string | Computed<string>;
  value?: string | Computed<string>;
}

export interface NodePool_NodePool {
  autoScale?: boolean | Computed<boolean>;
  count?: number | Computed<number>;
  gpuPartitionMode?: string | Computed<string>;
  id?: string | Computed<string>;
  labels?: unknown | Computed<unknown>;
  maxNodes?: number | Computed<number>;
  minNodes?: number | Computed<number>;
  name?: string | Computed<string>;
  nodes?: NodePool_NodePool_Nodes[] | Computed<NodePool_NodePool_Nodes[]>;
  size?: string | Computed<string>;
  tags?: string[] | Computed<string[]>;
  taints?: NodePool_NodePool_Taints[] | Computed<NodePool_NodePool_Taints[]>;
}

const NodePool_NodePool_TaintsFields: FieldMap = {
  effect: "effect",
  key: "key",
  value: "value",
};

export interface NodePoolConfig {
  /** A boolean value indicating whether auto-scaling is enabled for this node pool. */
  autoScale?: boolean | Computed<boolean>;
  /** The number of Droplet instances in the node pool. */
  count: number | Computed<number>;
  /** The AMD GPU partition mode for this node pool. Only applicable to AMD GPU sizes that support partitioning. Immutable after the node pool is created. When omitted, the GPUs in the pool are left unpartitioned. */
  gpuPartitionMode?: string | Computed<string>;
  /** An object of key/value mappings specifying labels to apply to all nodes in a pool. Labels will automatically be applied to all existing nodes and any subsequent nodes added to the pool. Note that when a label is removed, it is not deleted from the nodes in the pool. */
  labels?: unknown | Computed<unknown>;
  /** The maximum number of nodes that this node pool can be auto-scaled to. The value will be `0` if `auto_scale` is set to `false`. */
  maxNodes?: number | Computed<number>;
  /** The minimum number of nodes that this node pool can be auto-scaled to. The value will be `0` if `auto_scale` is set to `false`. */
  minNodes?: number | Computed<number>;
  /** A human-readable name for the node pool. */
  name: string | Computed<string>;
  /** The slug identifier for the type of Droplet used as workers in the node pool. */
  size: string | Computed<string>;
  /** An array containing the tags applied to the node pool. All node pools are automatically tagged `k8s`, `k8s-worker`, and `k8s:$K8S_CLUSTER_ID`. <br><br>Requires `tag:read` scope. */
  tags?: string[] | Computed<string[]>;
  /** An array of taints to apply to all nodes in a pool. Taints will automatically be applied to all existing nodes and any subsequent nodes added to the pool. When a taint is removed, it is deleted from all nodes in the pool. */
  taints?: NodePool_NodePool_Taints[] | Computed<NodePool_NodePool_Taints[]>;
  /** path parameter, not part of the API's own resource representation */
  clusterId: string | Computed<string>;
  /** path parameter, not part of the API's own resource representation */
  nodePoolId: string | Computed<string>;
}

export interface NodePoolAttrs {
  /** A boolean value indicating whether auto-scaling is enabled for this node pool. */
  autoScale: boolean;
  /** The number of Droplet instances in the node pool. */
  count: number;
  /** The AMD GPU partition mode for this node pool. Only applicable to AMD GPU sizes that support partitioning. Immutable after the node pool is created. When omitted, the GPUs in the pool are left unpartitioned. */
  gpuPartitionMode: string;
  /** A unique ID that can be used to identify and reference a specific node pool. */
  id: string;
  /** An object of key/value mappings specifying labels to apply to all nodes in a pool. Labels will automatically be applied to all existing nodes and any subsequent nodes added to the pool. Note that when a label is removed, it is not deleted from the nodes in the pool. */
  labels: unknown;
  /** The maximum number of nodes that this node pool can be auto-scaled to. The value will be `0` if `auto_scale` is set to `false`. */
  maxNodes: number;
  /** The minimum number of nodes that this node pool can be auto-scaled to. The value will be `0` if `auto_scale` is set to `false`. */
  minNodes: number;
  /** A human-readable name for the node pool. */
  name: string;
  /** The node pool's own real, current representation. (AI-inferred) */
  nodePool: NodePool_NodePool;
  /** An object specifying the details of a specific worker node in a node pool. */
  nodes: NodePool_NodePool_Nodes[];
  /** The slug identifier for the type of Droplet used as workers in the node pool. */
  size: string;
  /** An array containing the tags applied to the node pool. All node pools are automatically tagged `k8s`, `k8s-worker`, and `k8s:$K8S_CLUSTER_ID`. <br><br>Requires `tag:read` scope. */
  tags: string[];
  /** An array of taints to apply to all nodes in a pool. Taints will automatically be applied to all existing nodes and any subsequent nodes added to the pool. When a taint is removed, it is deleted from all nodes in the pool. */
  taints: NodePool_NodePool_Taints[];
  /** path parameter, not part of the API's own resource representation */
  clusterId: string;
  /** path parameter, not part of the API's own resource representation */
  nodePoolId: string;
}

export const NodePool: ResourceBinding<NodePoolConfig, NodePoolAttrs> = {
  wireType: "digitalocean_node_pool",
  fields: {
    autoScale: "auto_scale",
    count: "count",
    gpuPartitionMode: "gpu_partition_mode",
    labels: "labels",
    maxNodes: "max_nodes",
    minNodes: "min_nodes",
    name: "name",
    size: "size",
    tags: "tags",
    taints: {
      wireName: "taints",
      kind: "list",
      fields: NodePool_NodePool_TaintsFields,
    },
    clusterId: "cluster_id",
    nodePoolId: "node_pool_id",
  },
};
