// Code generated by `ubx sdk gen` from digitalocean_trigger. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface Trigger_ScheduledDetails_Body {
  name?: string | Computed<string>;
}

export interface Trigger_ScheduledDetails {
  /** Optional data to be sent to function while triggering the function. */
  body?: Trigger_ScheduledDetails_Body | Computed<Trigger_ScheduledDetails_Body>;
  /** valid cron expression string which is required for SCHEDULED type triggers. */
  cron: string | Computed<string>;
}

export interface Trigger_Trigger_ScheduledRuns {
  /** Indicates last run time. null value indicates trigger not run yet. */
  lastRunAt?: string | Computed<string>;
  /** Indicates next run time. null value indicates trigger will not run. */
  nextRunAt?: string | Computed<string>;
}

export interface Trigger_Trigger {
  /** UTC time string. */
  createdAt?: string | Computed<string>;
  /** Name of function(action) that exists in the given namespace. */
  function?: string | Computed<string>;
  /** Indicates weather the trigger is paused or unpaused. */
  isEnabled?: boolean | Computed<boolean>;
  /** The trigger's unique name within the namespace. */
  name?: string | Computed<string>;
  /** A unique string format of UUID with a prefix fn-. */
  namespace?: string | Computed<string>;
  /** Trigger details for SCHEDULED type, where body is optional. */
  scheduledDetails?: Trigger_ScheduledDetails | Computed<Trigger_ScheduledDetails>;
  scheduledRuns?: Trigger_Trigger_ScheduledRuns | Computed<Trigger_Trigger_ScheduledRuns>;
  /** String which indicates the type of trigger source like SCHEDULED. */
  type?: string | Computed<string>;
  /** UTC time string. */
  updatedAt?: string | Computed<string>;
}

const Trigger_ScheduledDetails_BodyFields: FieldMap = {
  name: "name",
};

const Trigger_ScheduledDetailsFields: FieldMap = {
  body: {
    wireName: "body",
    kind: "object",
    fields: Trigger_ScheduledDetails_BodyFields,
  },
  cron: "cron",
};

export interface TriggerConfig {
  /** Name of function(action) that exists in the given namespace. */
  function: string | Computed<string>;
  /** Indicates weather the trigger is paused or unpaused. */
  isEnabled: boolean | Computed<boolean>;
  /** The trigger's unique name within the namespace. */
  name: string | Computed<string>;
  /** Trigger details for SCHEDULED type, where body is optional. */
  scheduledDetails: Trigger_ScheduledDetails | Computed<Trigger_ScheduledDetails>;
  /** One of different type of triggers. Currently only SCHEDULED is supported. */
  type: string | Computed<string>;
  /** path parameter, not part of the API's own resource representation */
  namespaceId: string | Computed<string>;
  /** path parameter, not part of the API's own resource representation */
  triggerName: string | Computed<string>;
}

export interface TriggerAttrs {
  /** Name of function(action) that exists in the given namespace. */
  function: string;
  /** Indicates weather the trigger is paused or unpaused. */
  isEnabled: boolean;
  /** The trigger's unique name within the namespace. */
  name: string;
  /** Trigger details for SCHEDULED type, where body is optional. */
  scheduledDetails: Trigger_ScheduledDetails;
  trigger: Trigger_Trigger;
  /** One of different type of triggers. Currently only SCHEDULED is supported. */
  type: string;
  /** path parameter, not part of the API's own resource representation */
  namespaceId: string;
  /** path parameter, not part of the API's own resource representation */
  triggerName: string;
}

export const Trigger: ResourceBinding<TriggerConfig, TriggerAttrs> = {
  wireType: "digitalocean_trigger",
  fields: {
    function: "function",
    isEnabled: "is_enabled",
    name: "name",
    scheduledDetails: {
      wireName: "scheduled_details",
      kind: "object",
      fields: Trigger_ScheduledDetailsFields,
    },
    type: "type",
    namespaceId: "namespace_id",
    triggerName: "trigger_name",
  },
};
