// Code generated by `ubx sdk gen` from digitalocean_key. DO NOT EDIT.
import type { Computed, FieldMap, ResourceBinding } from "@ubx/sdk";

export interface Key_SshKey {
  /** A unique identifier that differentiates this key from other keys using a format that SSH recognizes. The fingerprint is created when the key is added to your account. */
  fingerprint?: string | Computed<string>;
  /** A unique identification number for this key. Can be used to embed a specific SSH key into a Droplet. */
  id?: number | Computed<number>;
  /** A human-readable display name for this key, used to easily identify the SSH keys when they are displayed. */
  name: string | Computed<string>;
  /** The entire public key string that was uploaded. Embedded into the root user's `authorized_keys` file if you include this key during Droplet creation. */
  publicKey: string | Computed<string>;
}

export interface KeyConfig {
  /** A human-readable display name for this key, used to easily identify the SSH keys when they are displayed. */
  name: string | Computed<string>;
  /** The entire public key string that was uploaded. Embedded into the root user's `authorized_keys` file if you include this key during Droplet creation. */
  publicKey: string | Computed<string>;
  /** path parameter, not part of the API's own resource representation */
  sshKeyIdentifier: string | Computed<string>;
}

export interface KeyAttrs {
  /** A unique identifier that differentiates this key from other keys using a format that SSH recognizes. The fingerprint is created when the key is added to your account. */
  fingerprint: string;
  /** A unique identification number for this key. Can be used to embed a specific SSH key into a Droplet. */
  id: number;
  /** A human-readable display name for this key, used to easily identify the SSH keys when they are displayed. */
  name: string;
  /** The entire public key string that was uploaded. Embedded into the root user's `authorized_keys` file if you include this key during Droplet creation. */
  publicKey: string;
  /** The SSH key's own real, current representation. (AI-inferred) */
  sshKey: Key_SshKey;
  /** path parameter, not part of the API's own resource representation */
  sshKeyIdentifier: string;
}

export const Key: ResourceBinding<KeyConfig, KeyAttrs> = {
  wireType: "digitalocean_key",
  fields: {
    name: "name",
    publicKey: "public_key",
    sshKeyIdentifier: "ssh_key_identifier",
  },
};
